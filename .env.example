GARMIN_EMAIL=
GARMIN_PASSWORD=
NOTION_API_KEY=
NOTION_PAGE_ID=
# Set to True to create Meals DB and run meal image analysis (requires Notion Plus and Mistral API key)
RUN_MEAL_ANALYSIS=False
# Only needed if RUN_MEAL_ANALYSIS=True
MISTRAL_AI_API_KEY=
MISTRAL_MODEL=pixtral-12b-2409
# Minutes between sync runs when running as daemon (default 60). Ignored if RUN_ONCE=1.
SYNC_INTERVAL_MINUTES=10
# SYNC_DAYS=30
